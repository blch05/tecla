# Solo si activás el login con Google en desarrollo local.
# En producción, estas credenciales se cargan en el panel de Supabase (Authentication → Providers → Google).
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
