import Ranking from '@/components/Ranking';

export const metadata = { title: 'ranking · tecla*', description: 'Los más rápidos de tecla*: rankings de hoy, de la semana y de siempre.' };

export default function RankingPage() {
  return <Ranking />;
}
